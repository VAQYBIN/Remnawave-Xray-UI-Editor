import { defineConfig } from 'vitest/config'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    /*
     * Потолок выше умолчания (5 с) не из-за медленных тестов, а потому что
     * умолчание стоит НИЖЕ их честной цены. Девять тестов идут 3.0–4.6 с в
     * одиночку: это React + jsdom + CodeMirror + React Flow, и дешевле они не
     * становятся ни от настроек userEvent, ни от чего-либо ещё — измерено.
     * Запас в 10–60% меньше обычного разброса машины, поэтому цвет полного
     * прогона сообщал о её загрузке, а не о состоянии кода: при двух
     * одновременных прогонах падало 6–9 тестов, ВСЕ в полосе 5023–6400 мс.
     *
     * 30 с — не «побольше на всякий случай», а втрое выше худшего измеренного
     * (6.4 с под двойной нагрузкой) и ровно то число, которое до этого уже
     * стояло точечно у трёх тестов с этим же объяснением в комментарии.
     * Зависший тест столько и подождёт: платят только падающие.
     */
    testTimeout: 30_000,
    include: ['test/**/*.test.{ts,tsx}'],
    setupFiles: ['./test/setup.ts'],
    css: false,
  },
})
